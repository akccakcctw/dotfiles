export LANG=en_US.UTF-8
export LC_COLLATE=C # LC_COLLATE set to C to make ls sort by alphanumeric

export KEYTIMEOUT=1

export VISUAL=nvim
export EDITOR=nvim
export GPG_TTY=$(tty)

export ASDF_NODEJS_LEGACY_FILE_DYNAMIC_STRATEGY=latest_available

# 本機專屬設定的逃生口（不進 repo）
[ -f ~/.zshenv.local ] && source ~/.zshenv.local
